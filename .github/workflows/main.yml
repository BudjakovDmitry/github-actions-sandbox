name: CI

on:
  push:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  ssh-connect:
    runs-on: ubuntu-latest
    
    steps:
      - name: Connecting via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 167.172.176.155
          username: root
          password: .:{5}!/Zq(K98Fk7E$eR
          script: whoami
