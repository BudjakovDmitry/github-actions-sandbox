name: CI

on:
  push:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "build"
  ssh-connect:
    runs-on: ubuntu-latest
    
    steps:
      - name: Connecting via SSH
        run: |
          ssh root@167.172.176.155
          .:{5}!/Zq(K98Fk7E$eR
          whoami
